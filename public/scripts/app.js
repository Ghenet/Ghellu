$(document).ready(function () {
    //GAME FUNCTIONALITY
    //Pseudo code
    //When user clicks the answer button and the answer id matches the question id, on click of check button, increment score by 1
    //If the answer button clicked is the wrong one, the check button turns red, and button value changes to 'try again'
    //Event listeners
    //Use Math.random();
});